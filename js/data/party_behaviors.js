(function(){
  // Accepted behavior identity only. Situation mappings and presentation belong to authored content.
  window.RAPartyBehaviors=Object.freeze([
    Object.freeze({id:'two-step',label:'TWO STEP'}),
    Object.freeze({id:'head-nod',label:'HEAD NOD'}),
    Object.freeze({id:'too-cool',label:'TOO COOL TO DANCE'})
  ]);
})();
