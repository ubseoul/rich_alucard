# ART SHIP 011 — Promotion evidence

The word *promotion* here means production staging only. It does not mean approved master or frozen status.

All nine exploratory candidate byte streams were copied unchanged into:

- `art_department/ships/art_ship_011/candidates/native/population/`
- `assets/before_the_fame/population/nightlife/`

For every asset, exploratory SHA-256 equals Ship-candidate SHA-256 equals production-path SHA-256. No regeneration, resizing, retouching, palette change, re-encoding or metadata-driven pixel rewrite occurred.

The production entries are registered as `CANDIDATE — HQ REVIEW REQUIRED`, excluded from default style authority, and have no runtime assignment. Exact records are in `PROMOTION_EVIDENCE.json` and `ART_SHIP_MANIFEST.json`.
