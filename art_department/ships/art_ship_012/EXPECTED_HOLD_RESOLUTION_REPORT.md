# ART SHIP 012 — Expected HOLD Resolution

The five candidates supply the missing actor/combat-object dependency on seven held surfaces:

- Portobello office: 1 screen (`portobello_manager`).
- NAIJA MART: 1 screen (`auntie`).
- Ocean floor: 2 screens (one reusable `soul` sprite).
- Training combat: 2 fights (`training_dummy`).
- Buckhead combat: 1 fight (`buckhead`).

This report is a forecast only. Current runtime truth remains **106 PASS / 15 HOLD**. No HOLD may move until HQ accepts and freezes exact pixels, Engineering integrates the mappings and real runtime Presentation QA passes. The manager candidate does not supply Portobello wife/children and cannot clear those separate dependencies.
