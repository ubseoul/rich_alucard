# ART SHIP 009 — Candidate Validation Report

**Result: PASS — 12/12 native candidates**

- Nine environment candidates are `270×480`, opaque RGB and exactly 30-color native reductions.
- The Hollow Bowl companion layer is `270×480` RGBA, binary alpha, exact-origin, and has no opaque pixels at or below the actor contact line `y=372`.
- Both seated states are `80×96` RGBA with binary alpha; each visible envelope is centered on `x=40` and its bottom opaque row is `y=88`.
- All 18 unique frozen/reference source paths match their recorded SHA-256 hashes.
- No frozen asset, runtime/gameplay file or path outside this Ship differs from base checkpoint `a16196e4fbc0c7530a00bdfe915fb9c9c7cd1826`.
- Three deterministic integer-scale internal review boards were generated.

Per-request checks and hashes are recorded in `CANDIDATE_VALIDATION_REPORT.json`, `CANDIDATE_TECHNICAL_FACTS.json` and `CANDIDATE_SHA256SUMS.txt`.

This technical PASS does not grant taste approval, APPROVED MASTER status, freeze, runtime integration or runtime acceptance.
