# ART SHIP 009 — Source Preservation Evidence

**Result: PASS — candidate and promotion checks**

- Base checkpoint: `a16196e4fbc0c7530a00bdfe915fb9c9c7cd1826`.
- 18 unique frozen/reference files were re-hashed from the live checkout; all match the Runtime Demand Map.
- Hollow Bowl base and Ship 008 crowd-layer bytes are unchanged.
- Tunde and Dre identity-anchor bytes are unchanged.
- The reused `tristan_apt` environment master is unchanged.
- Candidate-checkpoint scope inspection found no changed or untracked path outside `art_department/ships/art_ship_009/`.
- Promotion re-verification passes all 18 source hashes and the complete 199-file frozen corpus. The only non–Art Department additions are the 12 explicitly approved canonical PNGs.
- `lan_night → tristan_apt` remains zero-pixel reuse; the frozen source hash is unchanged and no `lan_night` bitmap was created.

Machine evidence is in `SOURCE_PRESERVATION_EVIDENCE.json` and `PROMOTION_VALIDATION_REPORT.json`. Candidate derivation used references read-only; promotion copied exact bytes without decoding or re-encoding.
